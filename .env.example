# Variáveis de ambiente do backend CuidarJuntos
# Copie para .env e preencha os valores. NUNCA versione o .env real.

# Assistente de IA (Anthropic) — necessária para o endpoint POST /api/v1/chat/.
# Sem esta chave, o chat responde HTTP 503 com mensagem amigável.
ANTHROPIC_API_KEY=

# Opcional: sobrescreve o modelo Claude usado pelo assistente.
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# Liga/desliga o assistente de chat. Mantenha desligado ("0") até existir
# consentimento explícito para o envio de dados a um provedor externo.
CHAT_ASSISTANT_ENABLED=0

# Celery — fila assíncrona (notificações, resumo semanal, etc.).
# Aponte para uma instância local do Redis em desenvolvimento.
CELERY_BROKER_URL=redis://localhost:6379/0

# Opcional: sobrescreve o backend de resultados do Celery. Se
# django_celery_results estiver instalado, o padrão já é "django-db";
# caso contrário, cai de volta para o mesmo Redis do broker.
CELERY_RESULT_BACKEND=redis://localhost:6379/0
